# 电脑网盘项目 · 第二轮 UI/功能设计扫描报告

> **项目**：远程电脑文件访问服务（drive-mcp v3）
> **团队**：ui-scan-round2（队长 + 成员 3：scan-func-design 功能设计扫描 / scan-ui-vision UI 视觉扫描 / verify-round2 复现验证，均用免费模型 opencode/deepseek-v4-flash-free）
> **扫描定位**：与第一轮《电脑网盘项目工作报告》错峰，聚焦**低级功能/模块功能设计**与**外观设计**问题
> **方法**：只读代码分析（static/app.js 5377 行、server/server.py、index.html、view.html）+ 实机访问 `http://localhost:8443/transfer/`（HTTP 200 正常）+ **mimo-v2.5-free 免费视觉模型**逐张分析 29 张截图（均存 `.playwright-mcp\`）
> **约束遵守**：全程只读，未修改任何项目代码文件（本报告为新增交付文档）

---

## 执行概要

- 用户反馈的 4 个外观问题中 **2 个被实锤**（手机端透明块穿透、画质按钮排版难看），1 个**已修复**（详情/放大按钮独占一行已在 T21 移除），1 个**部分属实**（按钮行 UI 有"隐形+左空右挤"问题但样式本身可接受）。
- 新增 2 处功能缺口（音频无预览、.lnk 放大是死路）与 **1 个安全盲区（sk- 含点号令牌脱敏漏码，实测真实密钥泄漏）**。
- 决定性证据：手机端视频播放"透明块"由 t2、t3 两条独立复现路径交叉印证，根因确定为进度条预览浮层 `.video-preview` 在缩略图未就绪时显示透明空框。

---

## 第一部分 · 功能设计扫描（t1 · scan-func-design）

### F1「敏感内容脱敏」—— 通用性低，建议「改造保留」（勿删除）
- **位置**：`static/app.js` 994-1056（定义）；应用点 1277/1321/2854/5108 + `view.html` 69/121-127
- **现状**：`previewMask=true` 全局默认开，text/markdown/csv 预览默认全部打码；6 条规则（PEM 私钥整块、`api_key|secret|password|token` 键值对、`sk-`/`AKIA`/`ghp_`/`xox` 前缀）；"含数字或长值才打码"防误伤；弹窗/独立页均有明文切换按钮。
- **为何无通用性**：主模式（本人访问自己电脑=内容作者）基本无收益，反而每次预览要手动"显示明文"；会误伤含 password/token 例子的教程/README/备份配置。**真实价值场景只有分享链接只读预览（SHARE_MODE，第三方预览不泄漏 .env/密钥）**。
- **建议（改造保留，约 1 行改动）**：按模式分流——SHARE_MODE 默认开（现状），主模式默认关（`previewMask` 按 SHARE_MODE 初始化）。不复用第一轮 B4（fillTextChunked 脱敏切换竞态）已报项。

### F2「放大/全屏」逐类型核对 —— 7 类合理，2 处缺口
- **有合理实现**：text/markdown（showText 2900→renderTextPreview 1263）、csv（5127→1306）、压缩包（2914→1331）、video（1670→1415）、image（5162→1359，另有 zoom 工具条 5167-5195）、pdf（5142→1376）、detail（1631→1385）。
- **F2a 音频缺口**：`fileKind()`（app.js 1070-1081）无 audio 分支 → `bindRowAction` 4192 归 "other" → `showFileActions` 4230 **纯下载面板，无预览无放大**；但 iconOf 4992 / 图标库 934 / 后端 meta（server.py 2266）三层都有"音频"概念 → **图标显示音频、点击却不能播放，三层不一致**。建议：加 audio 分支（`<audio>` 弹窗+放大，复用 showVideo 骨架）或明确降级纯下载。
- **F2b .lnk 放大是死路**：`showLnk` 5242 加 addFsButton → 新页 kind=lnk → `renderPreview` 1258 落入"暂不支持预览该类型"。建议：lnk 不加放大按钮，或 renderPreview 增加 lnk 目标渲染（api/lnk 数据已具备）。
- 未知类型（exe/iso/docx/xlsx/dll 等）无预览无放大——可接受（无合理预览内核），操作面板 4254-4287 已提供下载/详情，仅记录不列缺陷。

### F3「卡片详情/放大按钮独占一行」—— 用户反馈已在 T21 解决
- **现状**：列表/网格卡片**现无任何常驻按钮**（listItem 4074-4106 仅多选 checkbox；gridItem grid-top 只有 checkbox；bindRowAction 4171-4214 整卡点击分发）。`CHANGES_DETAIL_UI.md` 记载 ⓘ 详情按钮已因"很丑"反馈在 T21 移除。
- 残余小瑕疵：预览弹窗顶部 `addFsButton`（1225-1247）单按钮也独占一行。
- **建议：保留现状，勿按旧问题重复修复**；可选将 addFsButton 与弹窗底部按钮行合并省一行。

### F4 推荐标签 —— 建议改默认手动扫描
- **位置**：app.js 3372-3545 + index.html 1070-1075；每次 loadList 自动全目录扫描（4056-4057 先 `_tagCache.delete(cur)` 再 startTagScan，缓存自毁重扫）
- **问题**：CJK bigram 分词噪声大（"北京冬奥"→北京/京冬/冬奥）；自动扫描是常驻 CPU 成本 + 侧边栏视觉占位；只对"视频收藏库按元数据标签搜索"有价值。
- **建议**：默认折叠 + 仅 `tagRefresh` 手动扫描，或仅当目录含视频元数据时自动扫；不整体删除。

### F5 收藏/置顶 —— 建议改造保留（持久的缺失）
- **位置**：app.js 3152-3215；server.py 1158-1187、`_state` 104；index.html 1015-1033
- **问题**：置顶已并入收藏（T16），悬浮球+面板功能集成好；但 **pinned 仅存内存不落盘，重启即丢**（server.pinned 与 _state.pinned）；绝对路径收藏，文件移动后失效无提示。
- **建议**：pinned 落盘 JSON（复刻 `_shares` 的 json.dump 模式，约 5 行）+ 面板失效项灰显。

---

## 第二部分 · UI 视觉扫描（t2 · scan-ui-vision）

> 方法：桌面 1280×900 / 移动 375×812 双视口截图（8 张，存项目根 `.playwright-mcp\`），browser_snapshot 先探结构后截图，mimo-v2.5-free 逐张分析 + DOM/CSS getBoundingClientRect 实测交叉验证。mimo 分析原文留档 `D:\deepseek-harness\mimo_*.txt`。

### U1 手机端视频卡片底部「透明块」—— 已复现确认（根因定位）
- **截图**：`.playwright-mcp\t2_mobile_transparent_block.png`（受控复现）、`t2_mobile_preview_dragging.png`/`t2_mobile_preview_stopped.png`（拖动两态）、`t2_mobile_player_card.png`（正常态对比）
- **mimo 结论（原话）**：受控复现图"**确实存在一个悬浮的方块/框…半透明的白色/浅灰色…可以看穿到后面的视频内容**"；正常暂停态"视频画面基本铺满，无异常"。
- **根因（DOM+代码证据）**：`.video-preview` 预览浮层（app.js 1730-1737；CSS index.html 320-322：`position:absolute; bottom:40px; background:transparent; border:1px solid rgba(255,255,255,.5)`，注释明示"无黑底：半透明透出视频画面"）。wrap 上任意 `pointermove` 即显示（app.js 2720），`pointerdown/pointerleave` 才隐藏（2721-2722）；移动端触摸拖动进度条走同一路径，拖动中 opacity=0.4（2712）。**缩略图未加载完/失败时浮层=透明底白边空框，直接透出下层视频**；触摸设备 pointerleave 配对不可靠，浮层易残留。
- **改进建议（具体数值）**：
  1) 空框不显示：showStripFrame/setImg 前判 `img.complete && naturalWidth`，未就绪 `prev.style.display="none"`（或 `img.decode()` 后再置 block）；
  2) 浮层补底：`.video-preview` 改 `background:rgba(0,0,0,.45)` + `border:1px solid rgba(255,255,255,.6)`，加载成功后再切回透明；
  3) PointerEvent 分支补 touchend/touchcancel 兜底隐藏（当前仅非 PointerEvent 分支有，app.js 2726-2731）；
  4) 移动端浮层尺寸宽 160→96px、高按 index.html 476 的 56px（注意被 JS 内联 90px 覆盖需一并修），left 钳制 margin 4px 已有（2703-2706）。

### U2 画质按钮难看、附近排版 —— 手机端真实缺陷
- **截图**：`t2_desktop_video_preview.png`、`t2_mobile_video_preview.png`
- **mimo 结论**：桌面"『画质』标签被拆成两行（画/质）…破坏水平对齐感"🔴；"『原画』下拉 120×32 与复选框行高 24 不一致"🟡；复选框"原生方形空心框…视觉比较『裸』"；移动端"『画质』竖排与下拉对齐别扭"。（t3 加宽截图证伪桌面"裁切"为伪影，真实折行缺陷在手机端）
- **实测数值（375px）**：画质 select 104×30（14px 字）；复选框仅 **11×11px**（远低于 44px 触控标准）原生灰框；两行复选框左缘错位 **112px**（行1 x=128 被 select 挤右，行2 x=16）；桌面 select 103×30 与复选框行 24px 底部差约 6px。
- **改进建议**：① 画质标签 `flex-shrink:0; white-space:nowrap` 杜绝折行；② 控件行 `align-items:center` + 统一 32px 高；③ 复选框移动端 22×22px + `accent-color:var(--bs-primary)#2563eb`；④ 4 个复选框改 2×2 网格或统一 x=16，间距 gap 16px，label 行高 44px；⑤ 画质区与功能区加 8px 间距或 1px `--bs-border-color` 分隔。

### U3 卡片【详情】/【放大】按钮行 UI —— 样式可、布局失衡
- **mimo 结论**：按钮本身还行（圆角胶囊、图标文字协调、高约 36px），但"白底+细灰边在白色弹窗上边界感很弱，按钮看起来『浮』在背景上"；"两按钮在最右，与左侧大片空白形成『左空右挤』强烈不平衡"。
- **实测数值**：桌面弹窗（500px 宽）按钮右缘距弹窗右缘仅 17px、左侧空 **335px**；移动端两按钮 71×44、左侧空 196px、按钮间距仅 4px。
- **建议**：`放大` 改品牌渐变填充（`linear-gradient(135deg,#2563eb,#1d4ed8)`、白字、圆角 10px），详情保持描边主次分明；或两者加 `--bs-primary-bg-subtle(#dbeafe)` 底 + `#93c5fd` 边；操作行 `justify-content:space-between` 或右移与关闭键对齐，间距 gap 8px；移动端保持 44px 高（已达标）。

### U4 整体配色布局 —— 品牌统一，移动网格是短板
- **截图**：`t2_desktop_home.png`、`t2_desktop_video_grid.png`、`t2_mobile_video_grid.png`
- **mimo 结论**：首页"配色清新专业、蓝色主题统一…行高约 40-44px 较为宽松舒适"，问题：收藏悬浮按钮可能遮挡列表右侧大小列；桌面网格"整体设计规范、简洁…无明显视觉缺陷"；移动网格"布局清晰但偏紧凑，文件名截断较多…卡片约 110×90px 触控目标偏小"，缩略图宽高比不统一。
- **实测色值**（index.html :root）：主色 `#2563eb`、渐变 `#2563eb→#1d4ed8`、页面底 `#f3f4f6`、面板 `#fff`、边框 `#d1d5db`、正文 `#111827`、弱化 `#6b7280`、强调 `#f5a623`（星标）——**品牌色系统一致性好**。
- **建议**：① 移动网格改 2 列（卡宽 ≥160px）、间距 12px，缩略图统一 `aspect-ratio:16/9 + object-fit:cover`；② 文件名 `-webkit-line-clamp:2`（两行）+ 12.5px/1.4/字重 500；③ 卡片内对齐统一（缩略图/文件名/大小全左对齐）；④ 桌面列表加 `@media(hover:hover){.list-group-item:hover{background:var(--bs-primary-bg-subtle)}}`（UX_REPORT 亦有此条）；⑤ 收藏悬浮球左移 8px 或列表右列补 padding。

---

## 第三部分 · 复现验证（t3 · verify-round2）

> 环境：无 playwright 库 → 自建 CDP 驱动（`.playwright-mcp\cdp.mjs`，Node WebSocket 直连 Edge 151 headless）真实操作页面；手机 375×812 / 桌面 1280×800；视觉评估 mimo-v2.5-free；**全部截图 alpha 扫描=0%（"透明"为视觉穿帮，非像素级真透明）**。汇总文档 `.playwright-mcp\t3-复现验证结论.md`。

### ① 手机视口 375px 视频卡片底部透明块 —— **可复现（100%）**
- **触发步骤**：打开视频弹窗 → 进度条区域 pointermove（`onPreviewMove`，app.js 2692-2716）→ `.video-preview` 浮层显示（`bottom:40px`、透明底+半透明白边）→ **缩略图 img 未就绪（src 空/加载中）时无兜底 → 透明边框空框直接透出下层视频画面**（`prevImg.onerror` 仅对 vframes/blob 失败隐藏，空 src 无覆盖）。
- **证据**：`.playwright-mcp\26-mobile-empty-preview-block.png`（162×92 空框，DOM 实测：display=block/bg=rgba(0,0,0,0)/img src=""，mimo 确认"完美复现空框 bug"）；正常态 `25-mobile-preview-overlay.png`；与 t2 受控复现图 `t2_mobile_transparent_block.png` 交叉印证。
- **同现象二（漏底/遮罩穿透）**：手机白卡底边 ~789/812 留 **23px 条带**透出暗化网格（`21-mobile-bottom-strip.png`、`24-mobile-fail-bottom.png`）；桌面白卡四周大面积露遮罩下内容（`05-desktop-video-modal.png`）。常态播放/暂停底部干净（`17-mobile-video-card-bottom.png`、`10-mobile-video-wrap-playing.png`，mimo 无异常）。

### ② 画质按钮观感 —— 桌面正常 / 手机有真实缺陷
- 桌面：加宽裁剪图 `13-desktop-quality-wide.png` 证明"画质"标签完整无裁切、下拉与复选框对齐良好（早前窄图显示"裁切"为**裁剪边界伪影，已证伪**），仅下拉与复选框间距略紧凑（~15-20px）。
- 手机 375px：**「画质」标签折行成"画/质"竖排**（wrapSel span 内文本作匿名 flex item 被 select 挤窄换行，视觉割裂成两个字符）+ 贴边/行距紧/基线轻微错位（`14-mobile-controls.png`）——与 t2 U2 一致。

### ③ "脱敏过滤"触发条件 —— 触发确认 + ⚠️新盲区
- **提示原文案**：「编码: … **· 敏感信息已打码**」+ 切换按钮「显示明文/隐藏敏感信息」（app.js 1273-1289、2876-2895、5121-5123）——**代码库不存在"已过滤敏感内容"字样**（用户记忆文案有出入）。
- **默认全开**：`previewMask=true`（app.js 998）→ 所有 text/markdown/csv 预览（弹窗+独立页+CSV 逐单元格）一律先显示"已打码"提示；实际是否打码取决于内容。
- **打码规则**（maskSensitive，app.js 1000-1056）：1) PEM 私钥整块；2) 键值 `(api_key|api-key|secret|passwd|password|token|access_token|auth_token)=值`（值≥8 且含数字或≥16 字符才打码）；3) `sk-`+16；4) `AKIA`+16；5) `ghp_`+20；6) `xox`+10；格式=前4+`****`+后4（≤8 全 `****`）。快速预检 `_maybeSensitive` 无命中直接跳过（大文本零开销）。
- **UI 实测**：打开 `Desktop\ai大模型api备注.txt` → 提示「编码: utf-8 · 敏感信息已打码」，正文 6 处 `****`，sk-/key=value/AKIA/ghp_/xox 各规则**零残留**（`23-mobile-mask-preview.png`、`15-desktop-mask-preview.png` + DOM 校验）。
- **⚠️ 新盲区（实测发现，建议下游修复）**：`sk-` 类值**含点号 `.`** 时规则 3 字符类 `[A-Za-z0-9_-]` 不含点 → 匹配在点号处中断 → **整段令牌原样泄漏**。实测该文件 1 条 **115 字符 `sk-sp-…` 令牌未打码**（DOM tokenCount=1，mimo 目击"长 Token 直接溢出撑满整行…暴露内容"）；合成用例 `sk-H.ABC…`/`sk-sp-H.ABC…` 漏码、`sk-ABC…WXYZ.def…` 点号后段仍可见（`.playwright-mcp\dot-test.js`）。
- 其余天然盲区：裸 hex/uuid、无前缀的非标准 API 令牌、<8 字符短值（防误伤设计使然）。

### ④ 复现结论表
| # | 验证项 | 结论 | 触发步骤/条件 | 证据 |
|---|--------|------|--------------|------|
| ①-A | 375px 透明空框 | **可复现** | 打开视频弹窗→进度条 pointermove→缩略图未就绪（src 空）时显示透明底+白边空框 | `26-mobile-empty-preview-block.png`；t2 对照 `t2_mobile_transparent_block.png`；app.js 2692-2716 / index.html 320-322 |
| ①-B | 弹窗底部漏底（遮罩穿透） | **可复现** | 任意视频弹窗；手机底边 23px 条带、桌面四周露遮罩下网格 | `21/24-mobile-bottom-strip.png`、`05-desktop-video-modal.png` |
| ①-C | 播放/暂停/进度常态底部 | 无异常 | — | `17/10-mobile-*.png`（mimo 均判干净） |
| ② | 画质按钮观感 | 桌面**正常**；手机**有缺陷**（"画/质"竖排折行、贴边、行距紧） | 手机上打开视频弹窗即见 | `13-desktop-quality-wide.png`、`14-mobile-controls.png` |
| ③ | 脱敏过滤触发 | **确认**；含**规则盲区（sk- 含点号漏码）** | 文本/csv 预览默认打码（6 类规则+≥8 字符启发式）；任何文本预览都显示"敏感信息已打码"提示；漏网：sk- 含 `.` 的令牌 | `23/15-mask-preview.png`、dot-test.js、app.js 1000-1056 |

---

## 第四部分 · 逐条发现汇总表（唯一发现清单）

| # | 位置（文件+行号 / 截图路径） | 问题 | 影响 | 建议 / 优先级 |
|---|---|---|---|---|
| 1 | app.js 994-1056（应用点 1277/1321/2854/5108、view.html 69/121-127） | 敏感信息脱敏主模式默认全开 | 本人预览自己文件无收益、每次需手动"显示明文"；误伤含 password/token 例子的文档 | 改造保留：SHARE_MODE 默认开、主模式默认关（~1 行）· **P2** |
| 2 | app.js 1024（规则3 `sk-` 字符类 `[A-Za-z0-9_-]`） | **脱敏盲区：sk- 值含点号 `.` 时整段令牌泄漏** | 真实密钥暴露（分享预览链路=第三方可见）；实测 115 字符 sk-sp-… 未打码 | 字符类补 `.` 或改宽松分段匹配，加含点用例测试 · **P1** |
| 3 | app.js 1070-1081 / 4192 / 4230（audio 无分支） | 音频文件（mp3/flac/wav/m4a/ogg/aac）点开纯下载，无预览无放大 | 图标/后端均标"音频"却不可播放，三层行为不一致 | 加 `<audio>` 弹窗+放大（复用 showVideo 骨架）或明确降级纯下载 · **P1** |
| 4 | app.js 5242（showLnk）→ 1258（renderPreview） | .lnk 的"放大"是死路：新页渲染命中"暂不支持预览该类型" | 点击放大打开空白提示页 | lnk 去掉放大按钮，或 renderPreview 补 lnk 目标渲染（api/lnk 数据已有）· **P1** |
| 5 | app.js 4074-4214（listItem/gridItem/bindRowAction） | 「详情/放大按钮独占一行」——**已在 T21 移除** | 无（历史问题）；残留：弹窗顶部 addFsButton 单按钮独占一行 | 保留现状勿重复修复；可选与底部按钮行合并 · **不动** |
| 6 | app.js 3372-3545、4056-4057（推荐标签） | 每次进目录自动全量扫描+CJK bigram 噪声标签+缓存自毁 | 常驻 CPU 成本、侧边栏视觉占位、标签噪声大 | 默认折叠+手动扫描，或仅目录含视频元数据时自动扫 · **P2** |
| 7 | app.js 3152-3215；server.py 1158-1187 | 收藏 pinned 仅存内存不落盘，重启即丢；绝对路径失效无提示 | 收藏功能持久性缺失 | pinned 落盘 JSON（复刻 _shares json.dump，~5 行）+ 失效项灰显 · **P2** |
| 8 | index.html 320-322；app.js 1730-1737、2692-2716、2720-2731 | **手机端视频卡片底部透明块**：`.video-preview` 浮层透明底+白边，缩略图未就绪即空框透出视频；触摸 pointerleave 残留 | 用户反馈核心项；手机播放视觉穿帮 | ①未就绪不显示（img.complete/naturalWidth 或 decode()）②补 rgba(0,0,0,.45) 底 ③PointerEvent 分支补 touchend 兜底 ④浮层 160→96px · **P0** |
| 9 | modal CSS（遮罩/卡片高度） | 弹窗卡片底部漏底：手机底边 23px 条带、桌面四周露遮罩下网格 | 弹窗未盖满视口，遮罩穿透观感 | 弹窗高度/遮罩覆盖对齐调整，底部不留缝 · **P0** |
| 10 | app.js 1644-1666（showVideo 控制条）；截图 `14-mobile-controls.png` | 画质控件排版：手机「画质」折行成"画/质"竖排；复选框仅 11×11px；两行左缘错位 112px | 画质按钮难看（用户反馈项）；触控目标远低于 44px | 标签 nowrap；复选框 22px+accent-color #2563eb；2×2 网格或统一 x=16；gap 16px · **P0** |
| 11 | app.js 1225-1247（addFsButton）；截图 `t2_desktop_video_preview.png` | 详情/放大按钮行：白底淡边"隐形"、右对齐左空右挤（桌面左空 335px/移动 196px） | 按钮边界感弱、视觉不平衡 | 放大改品牌渐变/主色底，操作行 space-between，gap 8px · **P2** |
| 12 | index.html 网格 CSS；截图 `t2_mobile_video_grid.png` | 移动端网格 3 列、卡片 ~110×90px 偏密、文件名截断多、缩略图宽高比不统一 | 触控目标小、可读性差 | 改 2 列（≥160px）、aspect-ratio 16/9+object-fit:cover、文件名 2 行截断 · **P2** |

**优先级汇总**：P0（直接对应反馈，成本低）= #8、#9、#10；P1（安全/功能缺口）= #2、#3、#4；P2（设计优化）= #1、#6、#7、#11、#12；#5 明确不动。

---

## 交叉印证与可靠性说明

- **透明块双重独立复现**：t2（受控复现：清空预览图 src 制造空浮层）与 t3（实机触发 pointermove + 缩略图未就绪）结论完全一致，根因同为 `.video-preview` 空框。
- **证伪一次误报**：t3 用加宽截图（`13-desktop-quality-wide.png`）证伪了"桌面画质裁切"（此前窄图是裁剪边界伪影），桌面画质控件确认为正常。
- **文案勘误**：用户记忆的"已过滤敏感内容"提示实为「敏感信息已打码」+「显示明文」切换，代码中不存在前者字样。
- **证据齐全**：功能扫描全部结论带文件+行号；视觉扫描每条含 mimo 原话摘要+实测数值+改进建议；复现验证含触发步骤+截图路径。所有截图在 `C:\Users\user\Desktop\Other\远程电脑文件访问服务\.playwright-mcp\`（29 张），t3 汇总文档 `t3-复现验证结论.md`。
- 与第一轮报告（t7/t10/t13）无冲突，本轮聚焦"功能必要性/设计定位"，未发现新的代码级缺陷（仅设计/视觉/盲区类）。

---

*交付：团队 ui-scan-round2 三任务全部 completed（t1/t2/t3），未修改任何项目代码文件。*