# 电脑网盘项目工作报告

> **项目**：远程电脑文件访问服务（drive-mcp v3）
> **工作性质**：全面体检 —— 扫描理解 / 分模块缺陷审计 / 结果核验 / 优化方案 / 专业评审
> **执行方式**：AgentTeams 多智能体协作（统辖官 1 + 子代理 8，五阶段流水线，任务 19 项）
> **报告日期**：2026-08 项目体检查交（审计范围基于当前工作区代码）

---

## 0. 执行概要（Executive Summary）

本项目是一个基于 Python 标准库 `http.server` 的"手机网盘"式远程电脑文件浏览/传输服务（单端口双协议 HTTPS/HTTP、MSE 免证书视频播放、MCP stdio 工具、打包中心、分享体系等），功能已经非常完整（前后端合计约 1 万行，经历了 T2–T40 共 32 项迭代）。本次体检采用 **五阶段纵深流水线**：扫描 → 审计 → 核验 → 优化 → 评审，全程由分布式子代理完成，最终获得交叉印证一致、可落地的结论。

**核心判定：**

| 维度 | 结论 |
|---|---|
| 代码质量总体 | 中等偏好；功能完备但存在明显"单文件累积 + 快速迭代"特征（server.py 5325 行 / app.js 5377 行） |
| 高危缺陷 | **6 项**（均为真实缺陷，三审交叉确认，0 误报），最严重为「打包接口绕过根目录校验 → 任意文件外传」 |
| 中危缺陷 | **后端 22 项 + 前端 8 项 + UX 6 项** |
| 低危/草率设计 | 数十项（死代码、重复实现、巨型函数、文档失效等） |
| 安全模型总体 | **有效**（token 前缀校验、路径 realpath+commonpath 双重越界校验、分享完全隔离、静态资源三重复核均验证通过），缺陷集中在若干"漏网入口"而非整体失效 |
| 优化判定 | **批准实施**；统一建议 5 波次路线（W1–W5），必做范围约 6–7 天 |
| 根本性抉择 | **维持零构建、不引入框架/构建链**（个人使用项目的核心资产是"拷贝即用、离线、零依赖"） |

---

## 1. 项目全貌（阶段一 · 扫描分队，任务 t1–t3）

### 1.1 项目定位与技术栈

- **定位**：把整台电脑（默认所有固定磁盘）变成一个手机浏览器可访问的 HTTPS 网盘，支持浏览/上传/下载/打包/在线视频播放（MSE 免证书转码）/文本·Markdown·CSV·PDF 预览/压缩包在线解压/分享链接；同时通过自带行式 JSON-RPC（MCP stdio）暴露 `drive_start/pin/status/stop` 四个工具供 AI 客户端调用。
- **版本**：`VERSION = "3.0.0"`（server.py L62）。
- **依赖**：唯一硬依赖 `cryptography`（自签证书）；`ffprobe/ffmpeg` 可选降级；`faster_whisper` 可选 ASR；前端 Bootstrap 5 + highlight.js + 自制图标 + `cjk-normalize.js`（简繁归一化）。**零 Web 框架、零构建工具链**是刻意设计。

### 1.2 目录结构与规模

| 文件 | 实测规模 | 职责 |
|---|---|---|
| `server/server.py` | **5325 行 / 226 KB** | 全部后端：HTTP 服务器、路由、转码、打包、分享、证书、MCP、CLI |
| `server/mcp_stdio.py` | 240 行 | 零依赖 MCP stdio 桥（JSON-RPC 2.0，tools-only） |
| `server/templates/index.html` | 1130 行 / 75 KB | 主站模板（页面骨架 + 内联 CSS 约 900 行） |
| `server/templates/view.html` | 132 行 | 独立预览页（`#__shadow` 占位 DOM 模式） |
| `static/app.js` | **5377 行 / 230 KB** | 前端主逻辑（停用词表占了 858 行） |
| `static/cjk-normalize.js` | 35 行 / 28.5 KB | 简繁/日式新字体归一化映射 |
| `static/icons` | 30 个（line/color 双套） | 文件类型 SVG 图标 |
| `static/thumbs` | 277 个 jpg | 视频缩略图运行时缓存 |

> 仅核心代码即约 **1 万行**，加 Bootstrap/highlight 第三方资产与大量文档；`git log` 28 次提交、32 项功能迭代（T2–T40）。

### 1.3 后端架构（server.py 地图）

- **类（6 个）**：`_DriveHandler`(133)、`_DriveServer`(1558，ThreadingHTTPServer)、`_ArchiveAborted`(4189)；`mcp_stdio.py` 的 `ToolError`/`Tool`/`MCPServer`。
- **核心机制**：
  - **单端口双协议**：`get_request`(1573) 首字节嗅探 `0x16` → HTTPS，否则明文 HTTP；AF_INET6 双栈（`server_bind` 1603）。
  - **安全模型 = URL token + 路径越界校验**：随机 token 作路径前缀(1099)；`_resolve`(345)/`_resolve_share_path`(364) 用 realpath+commonpath 限定 roots；分享完全隔离(1096)、最长 7 天。
  - **MSE 媒体管线**：转码会话(2848→2912→2928→3023)→原画缓存(3261)→缓存治理(3078)→字幕(3430)→ASR(3490)→缩略图(2535)/缩略图条/单帧。
  - **打包中心**：`_build_archive_items`(4194)/`_scan_plan`(4228)/`_write_zip_entries`(4363)/`_stream_archive`(4410)/后台任务(4535)。
  - **系统集成**：证书(4808→4851)/防火墙(4966)/URL 与 IP 枚举(4906/4920)/CLI 看门狗(5263)。
- **运行期线程**：serve_forever、`_trans_sweep_loop`(5s 空闲回收)、`_cache_sweep_loop`(600s)、每任务打包工人、视频探测 `ThreadPoolExecutor(8)`、CLI 看门狗(10s×3)。

### 1.4 前端架构（app.js 地图）

约 60 个功能函数/模块，核心区域：
| 模块 | 行号 | 职责 |
|---|---|---|
| STOPWORDS | 4–861 | 中文停用词表（搜索/标签） |
| 图标库 | 895–945 | 文件类型图标 |
| 敏感信息脱敏 | 998–1056 | P1 脱敏功能 |
| 弹窗框架 | 1097–1247 | openModal/openPreviewModal |
| 独立预览页渲染 | 1249–1429 | render* 系列 |
| 视频播放器 | 1640–2716（MSE 2252–2524） | MSE 免证书播放/字幕/ASR/进度预览 |
| Markdown 渲染 | 2720–2809 | README/文本预览 |
| init | 3031 | 启动装配 |
| 推荐标签 | 3379–3545 | CJK bigram 分词推荐标签 |
| 模糊匹配 | 3551–3680 | LCS/滑窗 Damerau-Levenshtein/乱序 |
| 两阶段搜索 | 3764–3865 | 精确→异步模糊 |
| loadList | 4011 | 列表渲染 |
| 批量多选 | 4302–4545 | 长按/勾选 |
| 打包中心 | 4552–4937 | 任务面板/进度 |
| 分享工厂 | 5258–5369 | 分享创建 UI |

- **DOM/事件模式**：index.html 8 大区域（navbar/main/packPanel/packMiniBar/pinFab/pinPanel/appModal/sidebar）；view.html 用 `#__shadow` 占位(75–97) 防脚本中断；模块级全局 `let` 20+ 个；顶层 `onclick` 立即绑定约 38 处（仅 1 处有空值守卫）；内联 onclick（renderTask 4713/4727）；localStorage 6 个 `drive.*` 键。

### 1.5 文档与功能时间线（t3 成果）

- 通读 README(385 行)/FRONTEND_AUDIT/REVIEW_REPORT/TEST_REPORT/UX_REPORT + 28 份 `CHANGES_*.md`。
- 功能时间线 T2→T40 共 32 项（置顶、单端口双协议、分享、打包中心、MSE 播放器、推荐标签、收藏、脱敏、图片预览、独立预览页、手机双端适配、多格式解压、编码修复等）。
- **抽查 12 项文档声称的功能全部真实存在于代码** —— 无"声称但不存在"的虚假功能。

---

## 2. 缺陷审计结果（阶段二 · 审计分队，任务 t4–t8）

### 2.1 后端缺陷（t1 三审 + t4/t5/t6）—— 经 t9 交叉核验，**6 高 / 22 中 / 低危若干**

#### 高危（6 项，全部【确认】）

| # | 缺陷 | 关键位置 | 影响 |
|---|---|---|---|
| **H-1** | **POST /api/archive 打包路径绕过根目录校验 → 任意文件外传** | 1486–1491 → 4548–4562（对照 /dlzip L1450 有 `_resolve`） | 受限根部署=完整任意文件读取/外传，**击穿安全模型**（t4、t6 独立交叉印证） |
| **H-2** | **persist 转码会话内存缓冲永不消费 → 内存膨胀 + 转码被节流至 ~1.28MB/s** | `_trans_encode_thread` 2885–2896 | 大视频多并发=OOM 风险；1GB 输出最少 13 分钟（与 CPU 无关） |
| **H-3** | **转码/原画/ASR 缓存键仅 sha256(realpath)，不含 size/mtime → 源文件替换后长期服务陈旧内容** | 2588 / 3219 / 3468（对照 thumb L2549 含 size/mtime） | 重下/更新视频后静默播放旧帧内容 |
| **H-4** | **rar/7z 条目下载把用户可控 entry 原样拼进外部 CLI（开关/通配符/@listfile 注入）** | `_dl_7z` 4067 / `_dl_winrar` 4113 | 恶意包可篡改提取行为/下载错乱数据 |
| **H-5** | **p12 私钥网络分发 + 固定弱密码 "1234" + 明文落盘 + 同端口 HTTP 明文可嗅探** | /api/certp12 1355–1370 / 4886 | 私钥可被持 token 者经 HTTPS 下载；HTTP 明文可嗅探 |
| **H-6** | **import cgi 在 Python 3.13+ 已移除（PEP 594）→ 服务无法启动** | 21 / 1522 | 3.13+ 环境 MCP/CLI 双模式全不可用 |

#### 中危（22 项，全部【确认】，按域归纳）

- **并发/一致性（6）**：`_shares` 无锁竞态(runtime 崩溃/丢分享)；`/api/archive/cancel` 未取表锁 pop 与持锁迭代冲突；原画缓存 per-digest 锁横跨整段网络 I/O（同视频并发串行化）；原画区间谎报"已缓存"+清扫可删 → 短读/读零字节；下载中任务被 TTL/逐出清理；`_stop` 不清任务表 + `_start` 清扫删 `tk_*.zip` → stop→start 后任务失效。
- **资源/性能（5）**：转码/remux 无并发上限（任意数量满核 ffmpeg）；视频探测 8-worker 是"每执行器"上限非全局（连续 meta=1 叠加 ffprobe）；稀疏文件按 logical size 计缓存预算（单大视频打爆 2GB）；7z 列表每次全量重跑子进程全量缓冲；`_dl_7z` stderr 管道死锁。
- **上传（2）**：上传直接 `open(dest,"wb")` 覆盖同名文件 + 无大小/配额限制（磁盘耗尽 DoS）；文件名未过滤 Windows ADS 冒号（额备用数据流隐蔽写）。
- **安全纵深（3）**：安全响应头全缺 + token 在 URL 的 Referer 泄露/点击劫持无纵深；防火墙规则残留（看门狗 os._exit 跳过清理）；明文请求行/头无超时（slowloris 线程泄漏）。
- **媒体行为（3）**：30s 空闲回收与播放器暂停冲突（暂停>30s 续播 moof 不连续花屏）；transstatus 轮询不刷新 last_client；`_scan_plan` 文件分支无 reparse 检查（根外内容入包）。
- **可靠性（3）**：取消不即时（信号量与扫描阶段无检查）；看门狗 3s 超时连挂会误杀健康服务；Range 正则未锚定，多段 Range 静默只回第一段。

#### 低危（若干，均已验证）—— 代表项
`_resolve("")` 解析为进程 CWD、realpath 在 try 外刷 traceback、错误页一律 403、并发同名上传交错写、无 `do_HEAD`、分享深链 10 层上限、icons 放行任意 svg、点文件(如 `.gitignore`)无法下载、tar symlink Content-Length 失真、`X-Archive-Skipped` 头截断、同步 dlzip 2×IO+TOCTOU、`_thumb_path` 非原子写+无清理、三个进程存活期无界字典、ASR 请求线程内分钟级转写、FFPROBE 路径硬编码 `C:\Users\user\ffmpeg` 等。

### 2.2 前端缺陷（t7，经 t10 核验 20/20 确认，0 误报）

| 严重度 | 项 | 核心问题 |
|---|---|---|
| 高 | **A1** | `api()` 封装不查 `r.ok`/无超时（app.js 3015）；主模式 `loadList`(4021)/`init`(3055) 无 try/catch（分享模式 3036 却有，不对称）→ 断网/异常时**永久骨架屏/白屏** |
| 中 | A2 | renderTask 字符串拼接 onclick(4713/4727)，`esc()` 误用于 JS 上下文 → 受控数据注入通道（task_id 当前为后端 uuid 受控） |
| 中 | A3 | gridItem 封面 onerror 引号嵌套拼接 URL(4122–4127)，与 listItem 回退为两套机制 |
| 中 | B1 | `previewFile`(4158) 零调用=死代码，与 `bindRowAction`(4171) 逐行重复分发 |
| 中 | B2 | 弹窗版 show* 与独立页 render* 两套平行预览实现，行为已不一致 |
| 中 | B3 | `#__shadow` 占位 DOM(75–97) + 约 38 处顶层绑定仅 1 处有空白守卫 → 漏 id 独立页即崩 |
| 中 | B4 | fillTextChunked 脱敏切换并发竞态（可降中低） |
| 中 | B5 | view.html 图片页 wheel 监听器持久累积 |
| 中 | B6 | Blob URL 泄漏：`subVtt.url`(2177)/`asrVtt.url`(2240) 永不 revoke，`strip` 仅 onerror 时 revoke |
| 中低/低 | B7–B8, C1–C9 | `_tagCache` 缓存与 loadList 删除矛盾；模块级全局 let 20+；showVideo 1076 行巨型函数；fileKind/iconOf 两套分类表；STOPWORDS 858 行内联仅 1 处使用；三处分享 radio 重复；`view.html:70` `javascript:` 协议；renderTask 1s 全量重建 DOM 等 |

> **XSS 专项结论**：未发现可直接触发的存储型 XSS（文件名/元数据/CSV/文本均走 textContent/esc；renderMarkdown 先整体转义+协议白名单有效）。主要风险是"字符串拼接式 HTML 的脆弱注入通道"与"fetch 错误处理缺失"。

### 2.3 UX/文档缺陷（t8/auditor-ux-docs，经 t11 核验 17/19 确认 + 1 误报）

- **可访问性（高）**：文件列表行/网格卡/面包屑全部无 tabindex/role（app.js 全文件 grep `tabindex` 零匹配），**键盘与读屏用户完全无法操作**（仅 pinFab 键盘可达）。
- **对比度（中）**：5 组不达 WCAG AA（导航渐变白字 2.5~3.7:1、outline 蓝字 3.7:1、收藏星标 `#f5a623` 2.0:1 等；深色模式基本达标）。
- **触控/焦点（中）**：iPad 平板触控 34px<44px；窄屏刷新按钮无 aria 名称；sidebar 无焦点管理；view.html 按钮 33px + `window.close()` 依赖。
- **文档一致性（高）**：**README 全篇 20+ 处代码行号锚点系统性失效**（抽查 20 处全部失效，偏移 +21~+1711 行，如 `drive_pin` 声称 3807 实际 5131、`init` 声称 2229 实际 3031）；另 **9 项**文档与代码不一致（README 内部自相矛盾的解压描述、路由表缺 /api/img·/view·/api/unpackdir·show_hidden、§7 仍描述已移除的"置顶卡片"、行数声明全部过时、ETag 口径不一等）。
- **冗余资产（中）**：`static/locked.svg` 与 `icons/locked.svg` **逐字节相同**（268B，MD5 同），前者不在服务白名单、必 404、全项目零引用 → 死资产可删（30 双套图标为功能性设计非冗余）。

### 2.4 正面结论（防线有效性 —— 审计员确认）

- token 前缀严格校验、分享路由完全隔离；`_resolve`/`_resolve_share_path` 的 realpath+commonpath 对 `..`、符号链接/junction、`%2F/%5C` 编码分隔符均有效。
- 静态资源白名单+basename+normpath 三重复核；Range 边界计算正确 + Content-Disposition 经 `urllib.parse.quote` 防头注入；错误页 `html.escape` 防 XSS。
- 打包 arcname 无穿越（relpath+跨盘 basename 回退，无 `..`）；zip/tar 解压只读不落盘（namelist/getmembers 精确匹配）无写入穿越；`_scan_plan` junction 目录跳过 + visited/depth≤64 防环。
- MSE codec 契约、Range 解析、进程回收、SRT/ASS→VTT 均验证正确。
- 媒体管线**无死锁**（锁序无环）。

---

## 3. 结果核验（阶段三 · 核验分队，任务 t9/t10/t11）

采用"扫描员交叉核验审计员"的独立校验：3 名通读了对应源码的扫描员回读所指行号逐条 `read/grep` 复核，判定【确认/存疑/误报】。

| 核验 | 结论 |
|---|---|
| **t9 后端核验**（scanner-server，6 高 22 中） | **全部【确认】，0 误报 0 存疑**；行号/引文全部准确；附带 nuanced 澄清（H-3 的转码档重建自愈、真正静默陈旧在 original.cache 与 ASR vtt） |
| **t10 前端核验**（scanner-frontend，20 项） | **20/20【确认】，0 误报 0 存疑**；A2/A3/B3/B6 五条重点全部证据确凿；3 处修订建议（B4 降中低、C4 措辞、C9 属有意设计） |
| **t11 UX 核验**（scanner-docs，19 项） | **17【确认】+1【误报】+2 处小瑕疵**；README 行号锚点 20/20 失效结论成立（实测偏移 +21~+1711，t8 略高估最小偏移）；locked.svg 死资产完全确认 |

**核验总体**：三大审计分队的审计质量高，无"不存在的问题"被放大，唯一误报项（`_vthumbstrip_gen`"疑似改名"）已在核验中纠正（函数名存在，仅行号漂移）。

---

## 4. 优化建议与实施方案（阶段三 · 优化分队，任务 t12/t13/t14）

### 4.1 后端优化方案（t12，T12_PLAN.md）—— 分层 P0 快速修复 / P1 结构性 / P2 架构重构

**高 6 项方案要点：**

| # | 方案 | 优先级 |
|---|---|---|
| H-1 | **双保险根校验**：do_POST 路由层逐 path 过 `_resolve`（越界 403）+ `_archive_new_task` 加 roots + `_under` 防御（复用 5195），约 10 行 | **P0** |
| H-2 | 转码缓冲改为 `cfile is None` 才入 buf/节流（persist 落盘零缓冲）；完整版加 `mem_buf = not persist` | **P0**(建议直接上完整版) |
| H-3 | 缓存键不变，加 `src.info`(realpath+size+mtime) 失效比对 + `_src_changed` helper + 原子写 | **P1** |
| H-4 | 快速：拒绝 `-`/`@`/`*?`/`..` 开头 + 空 entry；完整：7z/rar 走 `_seven_list` 全量 Path 精确白名单 | **P0** |
| H-5 | p12 仅 HTTPS 提供、密码改 `secrets.token_urlsafe(8)` 随机并打印；完整版 icacls ACL + 一次性领取 | **P0** |
| H-6 | 护栏：try-import cgi，ModuleNotFoundError 时引导；完整：自实现流式 multipart（email 标准库兼容 3.10–3.13） | **P0/P2** |

**中 22 项方案**：`_shares` 加锁+快照写盘(P0)、`cancel` 补表锁(P0)、原画锁拆"锁内备数据/锁外写网络"+busy 登记(P1)、区间写成功才 merge(P1)、下载豁免 TTL/逐出(P1)、`_stop` 清表(P1)、转码并发 `_TRANS_SEM`(P1)、全局单例 `ThreadPoolExecutor(8)`(P1)、稀疏文件按区间和计费(P1)、省二次全量列表(P2)、`_dl_7z` stderr drain 线程(P0)、上传大小上限+同名拒绝/改名(P1/P2)、ADS 清洗(P0)、安全响应头+CSP(P1，CSP 实测后上线)、启动前删同名防火墙规则(P1)、明文头读取超时(P2)、暂停保活(P1)、transstatus touch(P1)、文件 reparse 检查(P1)、取消即时(P2)、看门狗双判定(P1)、Range 锚定+拒绝多段(P2)。

**实施 4 阶段**：阶段1(0.5–1 天，安全回归) → 阶段2(1.5–2 天，媒体) → 阶段3(1–1.5 天，打包+CSP) → 阶段4(2–3 天，重构)；附验收矩阵（并发脚本/pytest 基线约 20–30 用例/3.10–3.13 四版本矩阵）。

### 4.2 前端优化方案（t13）—— 贴合"零构建、纯静态、单文件"约束，三批渐进

- **批次1（P0 快速修复，一次提交）**：`api()` 改"永不抛"封装（AbortController+超时，返回 `{error}` 兼容 23 个调用点）；esc 加 null 守卫；gridItem 改 createElement 统一回退；showVideo 维护 blobUrls 统一 revoke；view.html paint 每次新建渲染容器；C9 卫生项。
- **批次2（P1 渐进重构，逐项独立提交）**：renderTask DOM 化 + addEventListener（消除 onclick 拼接）+ 增量更新；fillTextChunked 加 runId 令牌；`_tagCache` 加 force 语义；`on(id,ev,fn)` 空值守卫工具替换 38 处绑定；统一 fileCategory 合并两套分类表；抽 shareHoursForm 去三处重复；排序方向按钮；keydown 合并 setupGlobalKeys。
- **批次3（P1/P2 结构性，独立验证窗口）**：previewFile/bindRowAction 反向合并；双套预览内核合并（render 为内核、show 包壳）；previewMask 局部化；showVideo 分步抽离（parseVtt→renderMeta→MSE 外提 video-mse.js）；STOPWORDS 外置 stopwords.js（app.js 瘦身 16%）。
- **关键权衡**：**维持零构建**——文件拆分只用普通 `<script>` 顺序加载（不引 ES module）；明确挂起"全局 let 全量收敛"与"一次性大重构"（个人使用项目收益有限）。

### 4.3 UX/文档优化方案（t14）—— 12 项 P0/P1/P2

- **P0**：键盘可达性（listItem/gridItem `tabindex=0+role=button+keydown`，bulkMode `tabindex=-1`，面包屑改 `role=link`，axe 验收）；README 改版（**159 处行号锚点全部去锚改函数名引用**，补 4 项路由表，§7 置顶→收藏改写，行数/目录树更新，解压描述对齐）。
- **P1**：对比度全部收敛到 CSS 变量层（渐变 #2563eb→#1d4ed8、星标 #f5a623→#d97706 等）；iPad `@media (pointer:coarse)` 44px；refreshBtn 等补 aria-label；sidebar 焦点管理；view.html 按钮 44px + history.back() 兜底；删 locked.svg。
- **P2**：移动端 44px、@media print、面包屑改 nav/skip-link/tablist、统计口径统一。

---

## 5. 专业评审（阶段四 · 评审分队，任务 t15–t19）

### 5.1 三线方案评审总判定

| 评审 | 角度 | 判定 |
|---|---|---|
| **t15**（auditor-server-api） | 安全/API | **批准实施**；复核出 **3 个真实方案缺口**：H-4 快速版漏空 entry（实证 `entry=""` → `7z e -so archive ""` 整包流出）、H-4 白名单漏 WinRAR-only 分支、H-6 应"降级"而非"raise"；M14 注入点漏 2 处（`_send_static`/`_send_share_expired_page`）；M7 需对 MSE persist=False 豁免；M12 建议 `open(dest,"xb")` 原子拒绝覆盖；M13 保留名升必做 |
| **t16**（auditor-server-media） | 媒体管线 | **同意 5 + 部分同意 3 + 需补充 1**；H-2 判据核验成立（cfile 非 None ⟺ persist+start==0）建议直接上完整版；H-3 **漏第四条缓存 vthumbstrip/vframe**+ASR 须锁内比对；M7 播放应阻塞排队/下载 409；M8 保留 `_VIDEO_PROBE_LOCK`；**新增 .done 标记文件**（转码档回收后免重转，<15 行）升 P1 |
| **t17**（auditor-server-archive） | 打包/系统集成 | **批准实施，6 处修正**：H-4 快速版字符校验须只限 7z/rar 分支（防误伤 zip/tar 合法 `*?/`）；白名单版双条件+显示名→原始 Path 映射；M2 补 `_archive_dl`(4796) 第二调用点；M5 补下载硬超时兜底；M20 信号量改 `acquire(timeout=0.5)` 轮询；M15 规则名带 PID 升必选 |
| **t18**（auditor-frontend） | 前端兼容/重构策略 | **批准实施**；渐进三批正确（A1 单独先行）；补 10 处缺口（含 **view.html 的 script 列表须同步加 stopwords.js**、B6 revoke 须同步 removeTrack、loadVinfo/loadStrip 无 abort、AbortSignal.any 规避 Safari 15.4 前兼容坑等） |
| **t19**（auditor-ux-docs） | 综合/演进 | **批准实施**；合并 t12/t13/t14 为统一 **5 波次路线 W1–W5**，裁决 4 处交叉点（window.close 重复合并、A3 与 H1 同函数同批、C7 先于 M4、CSP 后置）；**四维度打分**排序；**维持零构建**结论；6 高 3 大统一顺序 |

### 5.2 多情境优缺点分析（评审核心，多角度）

评审员按要求评估了各方案在 4 种典型使用情境下的适用性与风险差异，矩阵如下：

| 情境 | 关键优先级差异 | 风险说明 |
|---|---|---|
| **纯家庭内网** | 全部 P0 无副作用照做；CSP 收益最低可后置 | 风险差异最小；token 在 URL + 内网，泄露面小 |
| **暴露公网/被扫描** | **H-1（任意文件读取=灾难）、H-5（扫描器抓 /api/certp12 是标准动作）、M14（Referer 防 token 外泄）、M16（slowloris/线程耗尽）** 优先级显著上调 | token 72bit 熵足够，前提是无泄露途径；Referer 是主要泄露面 |
| **多用户共享** | M1/M2（并发↑）、M12（用户间覆盖）、M13、M14 上调；M7 注意多人并发播放排队 | 覆盖/越权/竞态成为主要矛盾 |
| **低性能旧设备** | **H-2（内存+节流）、M7、M8、M9、M16** 上调；安全类 H-1/H-4/H-5 成本与性能无关照常 | 内存与 CPU 保护优先级上升；MSE 播放须豁免限流防回归 |

**统一 6 高 3 大顺序**（t19）：
①**H-1**（任意文件外传，最高优先）→ ②（H-6 护栏 + H-4 字符 + H-5 快速，同批）→ ③**A1**（前端骨架屏，并行）→ ④**H2 README 去锚**（并行）→ ⑤**H-2** → ⑥**H-3** → ⑦**H1 键盘可达**（随 A3 后置）→ ⑧ H-4 白名单/H-5 完整版 → ⑨ H-6 multipart（最后）。
**原则：止血 → 体验 → 合规 → 重构。**

### 5.3 "是否引入构建工具链/框架"这一根本性抉择（t19 专项）

- **结论：维持零构建、不引入。** 理由：
  1. "拷贝即用、离线、零依赖"是项目**核心设计资产**（单开发者、局域网私密服务）；
  2. 5377 行单文件的"熵"可**零构建化解**：STOPWORDS 外置（-16%）、showVideo 外提、双套预览合并、普通 `<script>` 顺序拆分；
  3. **npm 依赖链对局域网私密服务是新增供应链攻击面**；
  4. 折中"台阶"（推荐）：普通 script 拆分 + 纯函数 `node --test` 单测 + JSDoc 注释。
- **触发再评估的信号**：出现第二维护者 / 确实需要 npm 生态库 / 需要完整测试基建 —— 此时才值得考虑构建链/框架。

### 5.4 评审新增/强化的落地要点（三大评审员共识）

1. **H-4 必须补 `if not entry: 403`**（空 entry 会导致整包流式解出）。
2. **H-6 用"降级"而非"raise"**：`try import cgi except ModuleNotFoundError: cgi=None` + 上传路由 501 → 3.13 下服务其余功能可用。
3. **H-3 补第四条缓存 vthumbstrip/vframe** + ASR 比对放 per-path 锁内；转码档新增 `.done` 标记文件。
4. **M7 对 MSE persist=False 会话豁免限流**（播放拒绝=直接失败，前端无排队态）。
5. **M12 用 `open(dest,"xb")` 原子拒绝覆盖**，同时消除 check-then-write 竞态。
6. **M14 安全头注入补 `_send_static`/`_send_share_expired_page` 两处**；CSP 内联脚本外移后上线，`nosniff`/`Referrer-Policy` 先行。
7. **A3 与 H1（键盘）改同一 gridItem 函数 → 必须同人同批次**（A3 结构先行、H1 紧随）。
8. **view.html 的 script 列表须同步加 stopwords.js**（前端批次 3 易漏点）。
9. **M15 防火墙规则名带 PID（`TransferMCP-<port>-<pid>`）升为必选**，规避 os._exit 前 netsh 阻塞自杀。
10. **M10 反对 close-delimited 方案 b**（牺牲 Content-Length，进度/断点/代理兼容性差），推荐"复用列表 + 容量 LRU 缓存"。

---

## 6. 统一落地路线（t19 综合结论，W1–W5）

| 波次 | 内容 | 估算 |
|---|---|---|
| **W1** | 后端安全 P0（H-1 双保险、H-4 字符+空 entry、H-6 降级护栏、H-5 快速、M1/M2/M11/M13/M14 的 nosniff+Referrer） | ~1 天 |
| **W2** | 前端批次1（A1 先行 + 其余）+ README 去锚改版 | ~1.5 天 |
| **W3** | 后端 P1（H-2/H-3 完整、M3–M9/M17/M18、.done、M5/M6/M15/M19/M21）+ 前端批次2 | ~3–3.5 天 |
| **W4** | 后端 P2（H-6 multipart、M10、M16、M20、M22）+ 前端批次3 | ~2–3 天 |
| **W5** | 合并验收窗口（五情境冒烟、并发脚本、pytest 基线、3.10–3.13 矩阵、axe/Playwright） | ~1 天 |

**必做范围 W1–W3（约 6–7 天）为最小完整优化集；P2/批次3 为可选增强；总工作量约 9–10 天，风险全部受控。**

---

## 7. 风险与收益评估

### 7.1 优先止血清单（收益/成本比最高）

| 排序 | 事项 | 收益 | 成本 |
|---|---|---|---|
| 1 | H-1 打包根校验（约 10 行） | 修复"任意文件外传"灾难级漏洞 | 极小 |
| 2 | H-4 字符校验 + 空 entry | 修复 rar/7z CLI 注入 + 整包流出 | 极小 |
| 3 | H-6 cgi 降级护栏 | 修复 3.13+ 无法启动 | 极小 |
| 4 | H-5 p12 随机密码 + 仅 HTTPS + 一次性领取 | 修复私钥泄露面 | 小 |
| 5 | A1 前端 fetch 错误处理 | 修复永久骨架屏/白屏，核心可用性 | 小 |
| 6 | H-2 转码缓冲不消费 | 修复 OOM + 转码被节流 5.4× | 中 |

### 7.2 风险

- **本次审计揭示的已知风险未在本次修复范围内**（本报告为"体检报告"，非"落地修复"），实施顺序见 §6；已知风险均为**受控**、有明确修复方案与验收点。
- 修复引入的新回归风险（已识别）：H-1 403 文案暴露根边界（可接受）；H-5 前端需按协议隐藏证书按钮；M14 CSP 需实测后上线（先上 nosniff/Referrer）；M7 播放被拒（MSE 豁免后消除）；H-2/H-3 需 MSE 逐字节回归。

---

## 8. 对项目演进的建议（多角度）

1. **代码结构**：单文件 server.py(5325)/app.js(5377) 已达到可维护性临界点。建议在不引入构建链前提下做**低风险拆分**：后端按"打包/媒体/核心"拆 2–3 模块（不改变内部 API）；前端 stopwords.js 先行 + showVideo/视频播放器外提为独立 `<script>`。
2. **可测试性**：优先为纯函数建立 `pytest`/`node --test` 基线（`_resolve`/`_smart_decode`/`_original_cache_merge`/`_fix_zip_name`/Range 解析等约 20–30 用例），再动线程/会话等难测区域。
3. **文档维护**：README 去行号锚点（改函数名引用），建立"文档随码更新"约定，避免再出现 20 处失效锚点。
4. **Python 版本策略**：明确支持的 3.10–3.12 与 3.13 降级路径（cgi 移除）；`cert.not_valid_after_utc` 需 cryptography≥42，建议 `getattr` 兼容。
5. **渐进迭代**：贯彻 t19 的 W1–W5 波次与"止血→体验→合规→重构"原则，每波独立验证/回滚。

---

## 9. 结论

本项目功能完善、安全模型整体设计扎实（核心防线全部验证有效），主要问题集中在**长期单文件迭代积累的技术债**与**若干漏网的安全/可靠性入口**。经五阶段、19 项任务、8 名子代理（3 扫描 + 5 审计，并复用承担核验/优化/评审的交叉独立验证）**无"不存在的问题"被放大**：**6 高危 + 22 中危（后端）+ 20 项前端 + 19 项 UX 审计项**中，核验确认率约 97%、仅 1 项误报被纠正。

**最终结论：项目可持续演进，无需推倒重来；按统一 5 波次路线（W1–W5，必做约 6–7 天）实施优化即可显著消除安全与稳定性隐患，同时保持"零构建、拷贝即用"的核心设计资产。**

---

> **附注**
> - 本报告为团队体检交付物；原始佐证（行号、代码片段、逐条核验表）见项目根目录：`BACKEND_AUDIT.md`、`T4_AUDIT.md`、`T5_AUDIT.md`、`T6_AUDIT.md`、`T9_VERIFY.md`、`T12_PLAN.md`、`T15_REVIEW.md`、`T16_REVIEW.md`、`T17_REVIEW.md`，以及前端/UX/文档审计与核验内容（t7/t8/t10/t11/t13/t14）。
> - 执行流程：AgentTeams「netdisk-audit」团队，19 项任务（扫描3→审计5→核验3→优化3→评审5），依赖链自动解锁，交叉独立校验。
